name: Matrix Build Sultan + KowSU + SUSFS mini 

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  KERNEL_REPO: https://github.com/kerneltoast/android_kernel_google_tensynos.git
  SULTAN_TAG: "20260125"
  ANYKERNEL_REPO: https://github.com/TheWildJames/AnyKernel3.git
  PATCHES_REPO: https://github.com/yapixel/kernel_patches
  SUSFS_REPO_URL: https://gitlab.com/simonpunk/susfs4ksu
  SUSFS_REPO_BRANCH: sultan-shiba-susfs-minimal
  SCRIPTS_REPO: https://github.com/TheSillyOk/sh
  KSU_REPO: KOWX712/KernelSU
  KSU_BRANCH: master
  GCC_VERSION: "14.2.rel1"
  KSU_SUSFS_PATCH_URL: https://raw.githubusercontent.com/yapixel/cheetah_kernel_build/refs/heads/main/.github/patches/kowsu/10_enable_susfs_for_ksu.patch

jobs:
  build:
    name: Build ${{ matrix.codename }} Kernel
    runs-on: ubuntu-latest
    
    outputs:
      ksu_ver: ${{ steps.meta.outputs.ksu_ver }}
      s_tag: ${{ steps.meta.outputs.s_tag }}
      susfs_ver: ${{ steps.meta.outputs.susfs_ver }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - codename: gs201
            device_label: Pixel 7
            defconfig: gs201_defconfig
            anykernel_branch: sultan-gs201
            dtb_path: google-devices/gs201/dts

          - codename: zuma
            device_label: Pixel 8
            defconfig: zuma_defconfig
            anykernel_branch: sultan-zuma
            dtb_path: google-devices/zuma/dts

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install GCC
        run: |
          BASE_URL="https://developer.arm.com/-/media/Files/downloads/gnu/${{ env.GCC_VERSION }}/binrel/"
          ARCHIVE="arm-gnu-toolchain-${{ env.GCC_VERSION }}-x86_64-aarch64-none-linux-gnu.tar.xz"
          wget -q "${BASE_URL}/${ARCHIVE}" -O gcc.tar.gz
          tar -xf gcc.tar.gz
          echo "GCC_PATH=$GITHUB_WORKSPACE/arm-gnu-toolchain-${{ env.GCC_VERSION }}-x86_64-aarch64-none-linux-gnu/bin/aarch64-none-linux-gnu-" >> "$GITHUB_ENV"

      - name: Clone Source
        run: |
          git clone --depth=1 --branch "${{ env.SULTAN_TAG }}" "${{ env.KERNEL_REPO }}" sultan
          git clone --depth=1 "${{ env.ANYKERNEL_REPO }}" -b "${{ matrix.anykernel_branch }}" AK3
          git clone --depth=1 "${{ env.PATCHES_REPO }}" kernel_patches
          git clone --depth=1 "${{ env.SCRIPTS_REPO }}" sh
          git clone --depth=1 --branch="${{ env.SUSFS_REPO_BRANCH }}" "${{ env.SUSFS_REPO_URL }}" susfs4ksu

      - name: Metadata
        id: meta
        run: |
          set -euo pipefail
          S_TAG=$(git -C sultan tag --points-at HEAD 2>/dev/null | head -n1 || git -C sultan rev-parse --short HEAD)
          K_VER=$(./sh/get_ksuver.sh "KOWX712" "KernelSU" "${{ env.KSU_BRANCH }}" || echo "unknown")
          SUSFS_PATH="susfs4ksu/kernel_patches/include/linux/susfs.h"
          if [ -f "$SUSFS_PATH" ]; then
            SUSFS_VER=$(grep -E '#define SUSFS_VERSION' "$SUSFS_PATH" | awk -F '"' '{print $2}')
          fi
          SUSFS_VER=${SUSFS_VER:-"unknown"}
          echo "ksu_ver=$K_VER" >> "$GITHUB_OUTPUT"
          echo "s_tag=$S_TAG" >> "$GITHUB_OUTPUT"
          echo "susfs_ver=$SUSFS_VER" >> "$GITHUB_OUTPUT"
          echo "▶ Device           : ${{ matrix.codename }}"
          echo "▶ Sultan Kernel Tag: $S_TAG"
          echo "▶ KernelSU Version : $K_VER"
          echo "▶ SuSFS Version    : $SUSFS_VER"

      - name: Patch Kernel Source (KSU & SuSFS)
        working-directory: sultan
        run: |
          set -euo pipefail
          apply_patch_once() {
            local patch_file="$1"
            if patch -p1 --dry-run < "$patch_file" >/dev/null 2>&1; then
              patch -p1 < "$patch_file"
            elif patch -p1 --reverse --dry-run < "$patch_file" >/dev/null 2>&1; then
              echo "Patch already applied: $patch_file"
            else
              echo "::error::Patch failed or incompatible: $patch_file"
              exit 1
            fi
          }
          # 1. KSU Setup & Base Patches
          curl -LSs "https://raw.githubusercontent.com/${{ env.KSU_REPO }}/${{ env.KSU_BRANCH }}/kernel/setup.sh" | bash -s -- "${{ env.KSU_BRANCH }}"
          apply_patch_once "../kernel_patches/sultan/60_scripts_mkcompile_h.patch"
          apply_patch_once "../kernel_patches/sultan/61_scripts_setlocalversion.patch"
          apply_patch_once "../kernel_patches/sultan/62_init_Makefile.patch"
          
          # 2. SuSFS Setup & Patches
          mkdir -p fs include/linux
          cp ../susfs4ksu/kernel_patches/fs/susfs.c fs/
          cp ../susfs4ksu/kernel_patches/include/linux/susfs.h include/linux/
          cp ../susfs4ksu/kernel_patches/include/linux/susfs_def.h include/linux/
          SUSFS_PATCH="$GITHUB_WORKSPACE/susfs4ksu/kernel_patches/50_add_susfs_in_gki-android14-6.1.patch"
          
          apply_patch_once "$SUSFS_PATCH"
          
          # 3. 下载并应用外部远程 KSU SuSFS 补丁
          curl -LSs "${{ env.KSU_SUSFS_PATCH_URL }}" -o ../10_enable_susfs_for_ksu.patch
          (
            if [ -d "KernelSU" ]; then
              cd KernelSU
            elif [ -d "drivers/kernelsu" ]; then
              cd drivers/kernelsu
            else
              echo "::error::KernelSU directory not found"
              exit 1
            fi
            apply_patch_once "../../10_enable_susfs_for_ksu.patch"
          )

      - name: Inject Configuration
        working-directory: sultan
        env:
          CROSS_COMPILE: ${{ env.GCC_PATH }}
          CC: ${{ env.GCC_PATH }}gcc
        run: |
          set -euo pipefail
          DEFCONFIG="arch/arm64/configs/${{ matrix.defconfig }}"

          # 1. 注入核心增强基础网络配置
          cat >> $DEFCONFIG <<-'EOF'
          # Core Bootability & Performance
          CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y
          CONFIG_RUST=y
          CONFIG_LOG_BUF_SHIFT=21
          CONFIG_ANDROID_BINDER_IPC_RUST=m
          CONFIG_TMPFS_XATTR=y
          CONFIG_TMPFS_POSIX_ACL=y

          # Networking Configuration
          CONFIG_IP_NF_TARGET_TTL=y
          CONFIG_IP6_NF_TARGET_HL=y
          CONFIG_IP6_NF_MATCH_HL=y
          CONFIG_TCP_CONG_ADVANCED=y
          CONFIG_TCP_CONG_BBR=y
          CONFIG_NET_SCH_FQ=y

          # IPSet & TProxy Suite
          CONFIG_IP_SET=y
          CONFIG_NETFILTER_XT_SET=y
          CONFIG_IP_SET_MAX=65534
          CONFIG_IP_SET_BITMAP_IP=y
          CONFIG_IP_SET_BITMAP_IPMAC=y
          CONFIG_IP_SET_BITMAP_PORT=y
          CONFIG_IP_SET_HASH_IP=y
          CONFIG_IP_SET_HASH_IPMARK=y
          CONFIG_IP_SET_HASH_IPPORT=y
          CONFIG_IP_SET_HASH_IPPORTIP=y
          CONFIG_IP_SET_HASH_IPPORTNET=y
          CONFIG_IP_SET_HASH_IPMAC=y
          CONFIG_IP_SET_HASH_MAC=y
          CONFIG_IP_SET_HASH_NETPORTNET=y
          CONFIG_IP_SET_HASH_NET=y
          CONFIG_IP_SET_HASH_NETNET=y
          CONFIG_IP_SET_HASH_NETPORT=y
          CONFIG_IP_SET_HASH_NETIFACE=y
          CONFIG_IP_SET_LIST_SET=y
          CONFIG_NETFILTER_XT_TARGET_TPROXY=y
          CONFIG_NETFILTER_XT_TARGET_REDIRECT=y
          CONFIG_NETFILTER_XT_MATCH_SOCKET=y
          CONFIG_NETFILTER_XT_MATCH_MARK=y
          CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y
          CONFIG_NETFILTER_XT_MATCH_IPRANGE=y
          CONFIG_NETFILTER_XT_MATCH_OWNER=y
          CONFIG_NETFILTER_XT_MATCH_MAC=y
          CONFIG_NETFILTER_XT_MATCH_CONNTRACK=y
          CONFIG_NETFILTER_XT_MARK=y
          CONFIG_NETFILTER_XT_CONNMARK=y
          CONFIG_NETFILTER_XT_TARGET_CONNMARK=y
          CONFIG_IP6_NF_NAT=y
          CONFIG_IP6_NF_TARGET_REDIRECT=y
          EOF

          # 2. 注入 KowSU 核心依赖配置
          cat >> $DEFCONFIG <<-'EOF'
          CONFIG_KSU=y
          CONFIG_KPROBES=y
          CONFIG_HAVE_KPROBES=y
          EOF

          # 3. 注入 SuSFS 增强隐蔽逻辑配置
          cat >> $DEFCONFIG <<-'EOF'
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y
          CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_MAP=y
          EOF

          # 4. 生成与补全完整的 Kconfig 配置树
          make O=out CROSS_COMPILE="$CROSS_COMPILE" CC="$CC" ${{ matrix.defconfig }}
          ./scripts/config --file out/.config -d CONFIG_KSU_DEBUG
          make O=out CROSS_COMPILE="$CROSS_COMPILE" CC="$CC" olddefconfig

      - name: Build Kernel (Production)
        working-directory: sultan
        env:
          KBUILD_BUILD_USER: build-user
          KBUILD_BUILD_HOST: build-host
          CROSS_COMPILE: ${{ env.GCC_PATH }}
          CC: ${{ env.GCC_PATH }}gcc
        run: |
          make O=out CROSS_COMPILE="$CROSS_COMPILE" CC="$CC" -j"$(nproc --all)"

      - name: Package
        run: |
          set -euo pipefail
          cp sultan/out/arch/arm64/boot/Image.lz4 AK3/Image.lz4
          cat sultan/out/${{ matrix.dtb_path }}/*.dtb > AK3/dtb
          CLEAN_S_TAG=$(printf '%s' "${{ steps.meta.outputs.s_tag }}" | sed 's/[^A-Za-z0-9._-]/_/g')
          ZIP_NAME="Sultan-${{ matrix.codename }}-${CLEAN_S_TAG}-KowSU-SUSFS-mini-${{ steps.meta.outputs.susfs_ver }}+${{ steps.meta.outputs.ksu_ver }}.zip"
          (cd AK3 && zip -r9 "../$ZIP_NAME" . -x "*.git*")
          mkdir -p out_zip
          mv "$ZIP_NAME" out_zip/

      - name: Upload Zip Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Sultan-${{ matrix.codename }}
          path: out_zip/*.zip
          retention-days: 3
          if-no-files-found: error
          compression-level: 0

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/download-artifact@v4
        with:
          path: ./artifacts
          merge-multiple: true

      - name: Tag and Release
        env:
          GH_TOKEN: ${{ github.token }}
          KSU_VER: ${{ needs.build.outputs.ksu_ver }}
          S_TAG: ${{ needs.build.outputs.s_tag }}
          SUSFS_VER: ${{ needs.build.outputs.susfs_ver }}
        run: |
          set -euo pipefail
          DATE=$(date +"%Y.%m.%d")
          MAX_REV=$(git tag -l "${DATE}-r*" | sed "s/${DATE}-r//" | sort -n | tail -n1)
          REV=$(( ${MAX_REV:-0} + 1 ))
          TAG="${DATE}-r${REV}"
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git tag "$TAG"
          git push origin "$TAG"
          
          mapfile -t ZIP_FILES < <(find ./artifacts -name "*.zip" -type f | sort)
          if [ "${#ZIP_FILES[@]}" -eq 0 ]; then
            echo "No zip files found!"
            exit 1
          fi

          cat > release_notes.md <<EOF
          ### Build Information

          | Component | Details |
          | :--- | :--- |
          | **Devices** | gs201, zuma |
          | **Kernel Base** | Sultan ($S_TAG) |
          | **Manager** | [KOWX712/KernelSU](https://github.com/KOWX712/KernelSU) |
          | **KowSU Ver** | \`r$KSU_VER\` |
          | **SuSFS Ver** | \`$SUSFS_VER\` |
          | **Compiler** | GCC 14.2.rel1 |
          | **SuSFS Branch** | \`$SUSFS_REPO_BRANCH\` |

          ### Networking Features (Fortress)
          - **TCP Congestion:** BBR (v1) + FQ
          - **Routing:** Policy Routing (Table 1066) + IPSet (64k)
          - **Fixes:** ADDRTYPE Match, Google VPN (XFRM), TTL 64 Share
          > Built automatically with GitHub Actions matrix.
          EOF

          gh release create "$TAG" "${ZIP_FILES[@]}" \
            --title "Kernel Build — $TAG" \
            --notes-file release_notes.md
