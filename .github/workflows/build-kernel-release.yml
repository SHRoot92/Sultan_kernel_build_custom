name: Build Pixel 7/8/9 A16 Kernel — Sultan + xxKSU

on:
  workflow_dispatch:

# ---------------------------------------------------------------------------
# Shared environment variables used across jobs
# ---------------------------------------------------------------------------
env:
  ANYKERNEL_REPO: https://github.com/TheWildJames/AnyKernel3.git
  PATCHES_REPO: https://github.com/yapixel/kernel_patches
  SCRIPTS_REPO: https://github.com/TheSillyOk/sh
  KSU_REPO: backslashxx/KernelSU
  KSU_BRANCH: master
  GCC_VERSION: "14.2.0"
  GCC_ARCHIVE_URL: >-
    https://kernel.org/pub/tools/crosstool/files/bin/x86_64/14.2.0/x86_64-gcc-14.2.0-nolibc-aarch64-linux.tar.gz
  # 手动指定的 Sultan tag
  SULTAN_TAG: "20260125"

# ---------------------------------------------------------------------------
# Job 1: Build kernel and produce a flashable ZIP
# ---------------------------------------------------------------------------
jobs:
  build:
    name: Build zuma Kernel — Sultan + xxKSU
    runs-on: ubuntu-latest

    # Single device - no matrix needed
    env:
      CODENAME: zuma
      KERNEL_VERSION: "6.1.145"
      KERNEL_BRANCH: 16.0.0-sultan
      KERNEL_REPO: https://github.com/kerneltoast/android_kernel_google_tensynos
      ANYKERNEL_BRANCH: sultan-zuma
      DEFCONFIG: zuma_defconfig
      DTB_PATH: google-devices/zuma/dts
      DEVICE_LABEL: zuma

    outputs:
      sultan_tag:    ${{ steps.meta.outputs.sultan_tag }}
      ksu_ver:       ${{ steps.meta.outputs.ksu_ver }}
      zip_name:      ${{ steps.package.outputs.zip_name }}
      artifact_name: ${{ steps.package.outputs.artifact_name }}

    steps:
      # -----------------------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      # -----------------------------------------------------------------------
      - name: Install GCC ${{ env.GCC_VERSION }} cross-compiler
        run: |
          wget -q "${{ env.GCC_ARCHIVE_URL }}" -O gcc.tar.gz
          tar -xf gcc.tar.gz
          GCC_BIN="$PWD/gcc-${{ env.GCC_VERSION }}-nolibc/aarch64-linux/bin/aarch64-linux-"
          echo "GCC_PATH=${GCC_BIN}" >> "$GITHUB_ENV"

      # -----------------------------------------------------------------------
      - name: Clone kernel source and dependencies
        run: |
          git clone --depth=1 "${{ env.KERNEL_REPO }}" \
            -b "${{ env.KERNEL_BRANCH }}" sultan

          git clone --depth=1 "${{ env.ANYKERNEL_REPO }}" \
            -b "${{ env.ANYKERNEL_BRANCH }}" AK3

          git clone --depth=1 "${{ env.PATCHES_REPO }}" kernel_patches
          git clone --depth=1 "${{ env.SCRIPTS_REPO }}" sh

      # -----------------------------------------------------------------------
      - name: Checkout specific Sultan tag (${{ env.SULTAN_TAG }})
        working-directory: sultan
        run: |
          echo "Checking out Sultan tag: ${{ env.SULTAN_TAG }}"
          git fetch --tags
          git checkout "tags/${{ env.SULTAN_TAG }}" -b "build-${{ env.SULTAN_TAG }}"
          
          # 验证是否成功 checkout
          CURRENT_TAG=$(git describe --tags --exact-match 2>/dev/null || echo "no-tag")
          echo "Currently at: ${CURRENT_TAG}"
          
          if [[ "${CURRENT_TAG}" != "${{ env.SULTAN_TAG }}" ]]; then
            echo "::warning::Not on exact tag, but continuing build"
          fi

      # -----------------------------------------------------------------------
      - name: Collect build metadata (sultan tag, KSU version)
        id: meta
        run: |
          # 使用手动指定的 Sultan tag
          SULTAN_TAG="${{ env.SULTAN_TAG }}"
          
          # 验证 tag 是否存在
          if ! git -C sultan tag | grep -q "^${SULTAN_TAG}$"; then
            echo "::error::Sultan tag '${SULTAN_TAG}' not found in repository!"
            echo "Available tags:"
            git -C sultan tag | tail -20
            exit 1
          fi

          # Latest xxKSU short commit hash
          KSU_VER=$(./sh/get_ksuver.sh "backslashxx" "KernelSU" "${{ env.KSU_BRANCH }}" \
            || echo "unknown")

          echo "sultan_tag=${SULTAN_TAG}" >> "$GITHUB_OUTPUT"
          echo "ksu_ver=${KSU_VER}"       >> "$GITHUB_OUTPUT"

          echo "Sultan tag : ${SULTAN_TAG}"
          echo "xxKSU version : r${KSU_VER}"

      # -----------------------------------------------------------------------
      - name: Configure defconfig
        working-directory: sultan
        run: |
          DEFCONFIG="arch/arm64/configs/${{ env.DEFCONFIG }}"

          # ----- Networking / BBR / ipset configs ----------------------------
          EXTRA_CONFIGS=(
            CONFIG_TMPFS_XATTR=y
            CONFIG_TMPFS_POSIX_ACL=y
            CONFIG_IP_NF_TARGET_TTL=y
            CONFIG_IP6_NF_TARGET_HL=y
            CONFIG_IP6_NF_MATCH_HL=y
            CONFIG_TCP_CONG_ADVANCED=y
            CONFIG_TCP_CONG_BBR=y
            CONFIG_NET_SCH_FQ=y
            CONFIG_TCP_CONG_BIC=n
            CONFIG_TCP_CONG_WESTWOOD=n
            CONFIG_TCP_CONG_HTCP=n
            CONFIG_DEFAULT_BBR=y
            CONFIG_BPF_STREAM_PARSER=y
            CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y
            CONFIG_NETFILTER_XT_SET=y
            CONFIG_IP_SET=y
            CONFIG_IP_SET_MAX=65534
            CONFIG_IP_SET_BITMAP_IP=y
            CONFIG_IP_SET_BITMAP_IPMAC=y
            CONFIG_IP_SET_BITMAP_PORT=y
            CONFIG_IP_SET_HASH_IP=y
            CONFIG_IP_SET_HASH_IPMARK=y
            CONFIG_IP_SET_HASH_IPPORT=y
            CONFIG_IP_SET_HASH_IPPORTIP=y
            CONFIG_IP_SET_HASH_IPPORTNET=y
            CONFIG_IP_SET_HASH_IPMAC=y
            CONFIG_IP_SET_HASH_MAC=y
            CONFIG_IP_SET_HASH_NETPORTNET=y
            CONFIG_IP_SET_HASH_NET=y
            CONFIG_IP_SET_HASH_NETNET=y
            CONFIG_IP_SET_HASH_NETPORT=y
            CONFIG_IP_SET_HASH_NETIFACE=y
            CONFIG_IP_SET_LIST_SET=y
            CONFIG_NETFILTER_XT_TARGET_TPROXY=y
            CONFIG_NETFILTER_XT_TARGET_REDIRECT=y
            CONFIG_NETFILTER_XT_MATCH_SOCKET=y
            CONFIG_NETFILTER_XT_MATCH_MARK=y
            CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y
            CONFIG_NETFILTER_XT_MATCH_IPRANGE=y
            CONFIG_NETFILTER_XT_MATCH_OWNER=y
            CONFIG_NETFILTER_XT_MATCH_MAC=y
            CONFIG_NETFILTER_XT_MATCH_CONNTRACK=y
            CONFIG_NETFILTER_XT_MARK=y
            CONFIG_NETFILTER_XT_CONNMARK=y
            CONFIG_NETFILTER_XT_TARGET_CONNMARK=y
            CONFIG_IP6_NF_NAT=y
            CONFIG_IP6_NF_TARGET_REDIRECT=y
            CONFIG_IP6_NF_TARGET_MASQUERADE=y
          )

          for cfg in "${EXTRA_CONFIGS[@]}"; do
            key="${cfg%%=*}"
            sed -i -E "s/.*${key}=.*$/# ${key} removed by CI/g" "$DEFCONFIG"
            echo "$cfg" >> "$DEFCONFIG"
          done

          # ----- KernelSU configs --------------------------------------------
          cat >> "$DEFCONFIG" << 'EOF'
          CONFIG_KSU=y
          CONFIG_KSU_EXTRAS=y
          CONFIG_KSU_ALLOWLIST_WORKAROUND=n
          CONFIG_KSU_DEBUG=n
          CONFIG_KSU_KPROBES_HOOK=n
          CONFIG_KSU_TAMPER_SYSCALL_TABLE=y
          CONFIG_KSU_LSM_SECURITY_HOOKS=y
          CONFIG_KSU_THRONE_TRACKER_ALWAYS_THREADED=y
          EOF

      # -----------------------------------------------------------------------
      - name: Apply build-reproducibility patches
        working-directory: sultan
        run: |
          PATCHES=(
            ../kernel_patches/sultan/60_scripts_mkcompile_h.patch
            ../kernel_patches/sultan/61_scripts_setlocalversion.patch
            ../kernel_patches/sultan/62_init_Makefile.patch
          )

          for patch in "${PATCHES[@]}"; do
            if [[ -f "$patch" ]]; then
              echo "Applying patch: $(basename "$patch")"
              patch -p1 < "$patch" || echo "::warning::Patch $(basename "$patch") may have failed"
            else
              echo "::warning::Patch not found: $patch"
            fi
          done

      # -----------------------------------------------------------------------
      - name: Integrate xxKSU
        working-directory: sultan
        run: |
          curl -LSs \
            "https://raw.githubusercontent.com/${{ env.KSU_REPO }}/${{ env.KSU_BRANCH }}/kernel/setup.sh" \
            | bash -s -- "${{ env.KSU_BRANCH }}"

      # -----------------------------------------------------------------------
      - name: Build kernel
        working-directory: sultan
        env:
          KBUILD_BUILD_USER: build-user
          KBUILD_BUILD_HOST: build-host
          CROSS_COMPILE: ${{ env.GCC_PATH }}
          CC: ${{ env.GCC_PATH }}gcc
        run: |
          make O=out \
            CROSS_COMPILE="$CROSS_COMPILE" \
            CC="$CC" \
            ${{ env.DEFCONFIG }}

          make O=out \
            CROSS_COMPILE="$CROSS_COMPILE" \
            CC="$CC" \
            -j"$(nproc --all)"

      # -----------------------------------------------------------------------
      - name: Package AnyKernel3 flashable ZIP
        id: package
        run: |
          cp sultan/out/arch/arm64/boot/Image.lz4 AK3/Image.lz4
          cat sultan/out/${{ env.DTB_PATH }}/*.dtb > AK3/dtb

          SULTAN_TAG="${{ steps.meta.outputs.sultan_tag }}"
          KSU_VER="${{ steps.meta.outputs.ksu_ver }}"
          ZIP_NAME="Sultan-${{ env.CODENAME }}-${SULTAN_TAG}-xxKSU-r${KSU_VER}.zip"
          ARTIFACT_NAME="Sultan-${{ env.CODENAME }}-${SULTAN_TAG}-xxKSU-r${KSU_VER}"

          ( cd AK3 && zip -r9 "../${ZIP_NAME}" . -x "*.git*" "README.md" )

          echo "zip_name=${ZIP_NAME}"           >> "$GITHUB_OUTPUT"
          echo "artifact_name=${ARTIFACT_NAME}" >> "$GITHUB_OUTPUT"
          echo "ZIP_PATH=$GITHUB_WORKSPACE/${ZIP_NAME}" >> "$GITHUB_ENV"

      # -----------------------------------------------------------------------
      - name: Upload kernel ZIP as artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ steps.package.outputs.artifact_name }}
          path: ${{ env.ZIP_PATH }}

# ---------------------------------------------------------------------------
# Job 2: Create a GitHub Release from the built artifact
# ---------------------------------------------------------------------------
  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      # -----------------------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v6

      # -----------------------------------------------------------------------
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: ./artifacts

      # -----------------------------------------------------------------------
      - name: Create release with Sultan tag
        id: tag
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # 使用 Sultan tag 作为 release tag
          RELEASE_TAG="${{ needs.build.outputs.sultan_tag }}"
          
          echo "Creating release with tag: ${RELEASE_TAG}"
          
          # 检查 tag 是否已存在
          if gh api "repos/${{ github.repository }}/git/refs/tags/${RELEASE_TAG}" &>/dev/null; then
            echo "::warning::Tag ${RELEASE_TAG} already exists, deleting..."
            gh api "repos/${{ github.repository }}/git/refs/tags/${RELEASE_TAG}" -X DELETE
          fi
          
          # 创建新 tag
          gh api "repos/${{ github.repository }}/git/refs" \
            -X POST \
            -f "ref=refs/tags/${RELEASE_TAG}" \
            -f "sha=${{ github.sha }}" || {
              echo "::error::Tag creation failed"
              exit 1
            }
          
          echo "RELEASE_TAG=${RELEASE_TAG}" >> "$GITHUB_ENV"

      # -----------------------------------------------------------------------
      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2.6.1
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: Sultan Kernel ${{ env.RELEASE_TAG }}
          body: |
            ## Sultan Kernel Build for Pixel 7 (zuma)
            
            ### Build Details
            
            | Field   | Value |
            |---------|-------|
            | **Device** | Google Pixel 7 / Pixel 7 Pro (zuma) |
            | **Sultan Base** | [${{ needs.build.outputs.sultan_tag }}](https://github.com/kerneltoast/android_kernel_google_tensynos/releases/tag/${{ needs.build.outputs.sultan_tag }}) |
            | **KernelSU** | xxKSU `r${{ needs.build.outputs.ksu_ver }}` |
            | **Build Date** | $(date '+%Y-%m-%d %H:%M:%S UTC') |
            
            ### Features
            
            - Sultan kernel optimizations
            - xxKSU (KernelSU) integrated
            - BBR TCP congestion control
            - ipset support
            - Additional networking features
            
            ### Installation
            
            1. Download the ZIP file
            2. Flash in custom recovery (TWRP/PixelOS Recovery)
            3. Reboot and enjoy
            
            > ⚠️ **Warning**: Make sure you have a backup before flashing
            
            ---
            *Built automatically via GitHub Actions*
          files: artifacts/**/*.zip